# v0.2 工作安排

本文件只安排工作；设计由 [Workspace ADR](../adr/0005-workspace-learning-boundary.md)、[Document ADR](../adr/0006-canonical-document-generations.md) 与 contracts 拥有，实际证明由 [matrix](../acceptance/matrix.md) 拥有。工作分支为 `feat/workspace-v02`，不重写 v0.1.0 tag。

| 批次 | 依赖 | 当前安排 / 完成条件 |
|---|---|---|
| Workspace、学习 adapters | Harness 公开 membership | 已实现并验证隔离/迁移/重启；没有全局 picker 或 Agent courseId |
| Generations、PDF 本地链 | Workspace persistence | 已实现并验证 archive、页码、active-only search、历史 read、配额/恢复 |
| Vision / MinerU adapters | PDF / generation | 确定性 fake 与 capability gates 已验证；真实服务仍需独立环境验收 |
| 迁移 / 本地与 packed 学生路径 | 上述模块 | 已通过 offline v1 fixture、standalone、Chromium 与学习闭环；具体测试范围见 matrix |
| 数学显示 / 全局 review 修复 / 演示素材 | 当前学生路径 | 已本地实现、验证并分批提交；当前 checkout 与远端部署不等同 |
| 后续发布 | 用户明确授权 push/部署 | 先复核最终代码与对应gate，再push plugin、确认远端SHA、pin fork、push fork；当前本地修复入镜像后补Docker验收，不自动tag |

接下来只处理用户体验反馈及真实外部服务验收，未授权的发布不自动执行。具体下一步与精确本地/远端/镜像基线见 [CURRENT](../CURRENT.md)。

开工历史基线：插件 `5d55b58dcdfa215f777aceaec91bb0a97e7a47e5`、运行壳 `a76c5a0e7ba268284fa7f316aa2766d8f82747a1`；它们不是当前 HEAD。固定 Harness upstream/version 见 [COMPATIBILITY](../../COMPATIBILITY.md)。
