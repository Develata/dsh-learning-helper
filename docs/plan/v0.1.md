# v0.1 实施计划

| Phase | 依赖 | DoD | Test surface |
|---|---|---|---|
| P1 已验证：基础与 vertical slice | baseline + audit | docs、独立 bundle build/typecheck/test/pack；安装/dump/web boot；两错→weak→review→revision | unit、SQLite restart/failure/concurrency、Host integration |
| P2 已实现：课程证据与只读 Agent | P1 | 空 Course、独立 Source/Chunk/FTS、TXT/MD；稳定引用；真实 course_list/search/read 与 grounding policy | hash 去重、timeout/recovery、course isolation/locator、真实 registry/packed Host dispatch；LLM semantic 单列 |
| P3 实施中：Agent authoring backend | P2 | outline/quiz/plan draft validation，课程证据与学习闭环连接 | DAG/ownership/budget/retry/cancel；真实 packed 工具发布→评分→weak→review→v2→重启；LLM semantic 单列 |
| P4 学习 UI | P1 + P3 | 交互 quiz、feedback、弱点与计划变更解释 | browser flow、answer-key hiding、retry |
| P5 课程交付 | P4 | 完整 golden path + pinned tarball/profile/Docker compose | packed install、cold boot、3-day demo |

不把 OPTIONAL 集成放到 critical path；达到最低文档控制面后立即编码，计划不重定义架构。P2 Hard Gate 为 TXT/MD → Agent retrieval；PDF/MinerU/OpenFile 暂缓，UI/Docker 不进入本轮。P3 不增加 infrastructure 或 client UI；连接 Evidence → outline → initial plan → quiz → 既有 P1 submit。完整 MVP 继续由 P3–P5 实现。
