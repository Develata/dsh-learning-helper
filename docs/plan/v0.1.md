# v0.1 实施计划

| Phase | 依赖 | DoD | Test surface |
|---|---|---|---|
| P1 已验证：基础与 vertical slice | baseline + audit | docs、独立 bundle build/typecheck/test/pack；安装/dump/web boot；两错→weak→review→revision | unit、SQLite restart/failure/concurrency、Host integration |
| P2 已验证：课程证据与只读 Agent | P1 | 空 Course、独立 Source/Chunk/FTS、TXT/MD；稳定引用；真实 course_list/search/read 与 grounding policy | hash 去重、timeout/recovery、course isolation/locator、真实 registry/packed Host dispatch；LLM semantic 单列 |
| P3 已验证：Agent authoring backend | P2 | outline/quiz/plan draft validation，课程证据与学习闭环连接 | DAG/ownership/budget/retry/cancel；真实 packed 工具发布→评分→weak→review→v2→重启；LLM semantic 单列 |
| P4 已验证：学生 Web UX | P1 + P3 | 原生 Learning panel、课程设置/上传、Quiz/feedback、Progress/Plan/Why changed | packed Chromium、普通 UI answer-key 隔离、失败/重试/刷新/课程切换、视觉自审 |
| P5 已验证：课程交付 | P4 | 真实模型 QA/注入/authoring、固定 tarball/profile/Docker、README/DEMO/release review | 100 tests、packed Chromium、standalone、no-cache/cold boot/restart、语义检查 |

v0.1.0 发布基线保持冻结。用户体验期间明确授权了品牌替换与按计划任务建立学习会话；当前工作区进度见 CURRENT，不能把新功能算入旧 release SHA 的验证。任务会话 DoD：新会话自动发送、原草稿保留、模型继承、多会话入口、刷新/失败重试；通过 client tests 与 packed Chromium 验证。其余 UI/算法调整等待汇总反馈；暂不录制。PDF/MinerU/OpenFile、外部 PKM、FSRS、多用户均 deferred。
