# 当前状态

- Current phase：用户体验反馈实施；插件和独立品牌 worktree 位于 `feat/learning-helper-brand`，原运行壳 checkout 位于 master。本地品牌、任务会话、Agent 流程优化保留；本轮只完成原运行壳部署端口 hotfix。其余 UI/算法事项等待反馈；不录视频，不启动 P6。
- Last released baseline：plugin `958cf67627736232d06a9eeee70cdcb2c0369248` / annotated v0.1.0；fork `8a47b6d55f6c603d4c8b90159b30f361e228611b`；Harness upstream `c291e7961a515f6d7af9304e7fd1d257929aef26` / 0.1.5-rc.2。后续改动已在本地分批提交，未 push；release/Docker lock 未更新。
- What changed：七工具保持不变。learning_state_get 新增准备阶段、source 状态、最新 10 个练习摘要和总/未交数量；Learner State 与练习摘要只读一次学习 snapshot。模型 render 省略重复 refs/内部统计，完整 canonical value 与课程引用保留。单一 grounding policy 区分普通问答、学习动作、继续/新练习，并明确读取复用、失败分类、停止与恢复；opaque ID 错误提供复制真实 ID 的提示。详见 [Agent contract](contracts/agent-tools.md)。
- Existing local work：品牌 slots/标题/favicon/文案和任务“新会话/继续/多会话”保持。任务 opener 来自 AI 计划数据 + 可信模板，沿用 workspace/preset/模型，原 composer 草稿保留。Harness 持久化会话；浏览器书签与 stable sessionId/requestId 支持 30 秒超时、响应丢失后手动重试与刷新继续。清除浏览器数据/换 origin 后书签不保留，聊天仍在 Harness。
- What works：实际 packed Harness/Chromium 的 Course/上传/Authoring/Quiz/Weak/v2/刷新、双库重启、工具卡片、任务多会话及丢响应恢复均 PASS。纯领域 grading/适应策略、两库 ownership、Host routes、当前客户端功能未因 Agent 优化改变。
- Measured：两概念 golden fixture 中模型可见状态从 3514 到 2914 UTF-8 bytes（约 17%），同时增加准备/练习元数据；完整 canonical 仍通过注册 schema。此数值不是 token 或端到端耗时改进百分比。读取次数测试确认 context/quiz summary 共用一次 aggregate get；底层 store 的防御性复制未重构。
- Review fixes：避免新增摘要造成第二次完整 aggregate 读取；保留工具卡片需要的字段与 untrusted 标签。真实模型曾在资料不足后展开外部证明/提纲，以及抄错 chunk ID 后调用 bash；已明确停止分支与 opaque-ID 修正说明。修正后的 QA/不足/注入，以及最后 plan/quiz 定向复核通过；未降低引用校验或放行 shell 来使测试变绿。
- Verification（2026-09-13）：typecheck、116 tests（97 backend/script + 19 client）、build/pack PASS；三个 deterministic demos PASS，最后影响到的 demo:authoring 重跑 PASS。`LH_BRAND_SMOKE=1 pnpm run test:integration -- /home/deve/gitclone/learning-helper-brand` PASS；无 sibling 临时目录 frozen install/typecheck/116 tests/build/pack PASS；CodeGraph sync/新入口影响检查及 git diff --check PASS。回执在 artifacts/agent-flow-review.json、agent-flow-tests.log、agent-flow-standalone.json、integration-result.json、browser/result.json，不入 Git。
- Real LLM scope：真实 pinned Harness + packed plugin + 现有 newapi/gpt-5.6-luna，未直接调用模型 SDK。QA/insufficient/injection 的最终 grounding 结果在 artifacts/llm-agent-flow-grounding.json；opaque-ID 提示修改后的 plan/quiz 在 artifacts/llm-acceptance.json，fullSuite=false，已逐题/计划复核。前一次全套尝试中的 authoring 失败仍留存，不能把分批结果冒充一次全套 release acceptance。普通 tests/browser 仍使用 deterministic fixture。
- Limits：检索/重试次数的文字规则不是硬执行器；资料不足样本实际用了 4 次不同搜索，超过建议的 3 次。确定性证明保证 schema/ownership/幂等/资源上限，有限真实样本不保证未来模型行为。Skill/MCP 已核实为 Harness 公开能力，本轮未添加外部服务或依赖；建议学科教学方法以后按需 Skill，真正外部能力才配 MCP。没有重跑 Docker cold boot 或全 Harness GUI；P5 历史结果见 [final delivery](acceptance/final-delivery.md)。
- Deployment hotfix（2026-09-13）：原运行壳 deploy/learning-helper 使用 LEARNING_HELPER_PORT 同时配置 loopback 映射和 open.mjs 登录 URL，默认 3010；容器内部端口和认证不变。6 项部署测试、隔离 Docker 缓存构建、3010/3011 实际映射和登录、401/403 检查 PASS；回执为 artifacts/port-hotfix-result.json。该定向验证不是一次新的 no-cache 全产品验收。部署 README 中英文已同步，运行壳 doc-sync 34/34、CodeGraph sync/入口检查、两仓 git diff --check PASS；已提交至原运行壳 master（a76c5a0e7b），未 push 或替换用户容器。
- Runtime boundary：本轮 Harness packages/apps 新增修改为 0。先前品牌 worktree 仍只有登记的 9 个前端源码/资源 + 11 个测试/快照变更。原 checkout 的 Compose 手改 33333 已由可配置端口替代；正在运行的 `127.0.0.1:33333` 容器 ID、启动时间与映射未变，用户数据库未切换，本地新功能和端口 hotfix 尚未在该实例生效。
- Local commits：插件 feat/learning-helper-brand：6576be1 品牌 slots、c43b7bc 任务会话、5e5bb82 Agent 流程；运行壳 feat/learning-helper-brand：ba7650b558 品牌呈现；运行壳 master：a76c5a0e7b 端口 hotfix。运行壳两分支尚未合并，标签与远端 release 不变；本文件只记录代码提交，文档提交身份由 Git 拥有。
- Commit verification：品牌和任务会话的分批暂存快照分别在无 sibling 临时目录 frozen install/typecheck/build PASS；任务快照另通过 19 项 client tests。完整实现/测试文件与先前 116 tests/packed integration 验证内容逐文件哈希一致，未为 commit 重跑 LLM 或整个产品回归。两次 fork commit 的原有 lint/whitespace/vendor/署名或翻译 hook 均通过；品牌 worktree 缺失的锁定可选依赖经 SHA-512 校验补齐，无依赖清单/lock 变更、无 hook 绕过。
- Next 3 tasks：① 用户继续体验并汇总剩余反馈；② 用户决定体验开发版时明确分支整合和构建/部署范围；③ 后续明确授权后 push。本轮仅本地 commit，不自动重启、部署或发布。
