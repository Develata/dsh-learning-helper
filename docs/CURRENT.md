# 当前状态

- Current phase：P3 Agent Learning Authoring backend Hard Gate 已通过。空 Course → TXT/MD Evidence → grounded outline → unknown ConceptState → initial StudyPlan → grounded Quiz → student submit → weak → ReviewQueue → PlanRevision 已由确定性测试和真实 packed Harness Agent 工具 dispatch 贯通。P4 学生 UI 尚未实现。
- Last known good implementation commit：`ce482815c4302a3bbcfa7d5ffe5f349fb1d0605d`，包含 authoring、七工具、packed backend loop 与 adversarial 边界测试。最终交付版本为本文件所在插件提交（git rev-parse HEAD）；fork 的 Compatible plugin SHA 在其 UPSTREAM_BASE.md 固定到最终已测试且已推送的插件提交，避免循环 SHA 引用。
- What works：CourseAuthoringService 解析真实 Evidence refs，再由 LearningService 原子发布。严格 drafts、DAG、同课程引用、预算/数量限制、自动 IDs/time/version/status、语义幂等；四读三发布工具，单一 grounding/authoring policy 与轻量数学分析 guidance。Quiz public API/tool result 不含 correctOption/explanation。原 P1/P2 所有测试保持通过。
- Review / fixes：已完成 adversarial self-review。接入时修正 Harness presenter 不支持 write 枚举的问题（使用公开 edit 枚举）；修复 docs 中“全部工具只读/提案尚未实现”等语义漂移。补验真实写队列取消、并发不同草稿仅一份成功、满 200 quizzes 仍确认原请求、100 个不同引用、每概念 8 个大片段和 1..14 天/50 tasks 上限。复审未发现当前 scope 内已知 correctness failure；未把 policy 测试当作模型行为证明。
- Active decisions：Learning/Evidence 仍独立所有权与 schema version 1，P3 没有新增 durable 字段/command ledger/跨库事务。Source 不删除，authoring 只读 evidence；grading/adaptation 不调用 Evidence。Outline 与初始 plan 发布后固定，相同 v1 重试即使已有 v2 也只返回原 v1；完全相同 Quiz 不生成新 quizId。Agent 不能写 mastery/Attempt/PlanRevision。
- Last verification（2026-09-12；Node 24.18.0 / pnpm 11.7.0）：typecheck PASS；tests **85/85**（69 baseline + 16 P3）；build PASS；pnpm demo / demo:evidence / demo:authoring 全 PASS；pack PASS；git diff --check PASS；本地 Markdown 链接检查无失效目标。三种 demo 均为 deterministic，authoring demo 使用真实 ToolRuntime。
- Harness integration PASS：`pnpm run test:integration -- /home/deve/gitclone/learning-helper` 验证 prebuilt tgz install、config dump、authenticated Web 资源、standard preset 的真实 Agent 作用域七工具 dispatch/prompt assembly、源资料导入/回读/引用、outline/plan/quiz 发布、HTTP student submit、两错→weak/review/v2/20min/3题，以及两个独立 Harness 进程后的双库恢复和重复发布/提交。原 P1 demo course 同时验证。运行回执 artifacts/integration-result.json 记录实际 plugin/fork SHA、dirty 标志及 tarball SHA-256；不是浏览器交互或自主 LLM 验收。
- Standalone PASS：无 sibling checkout 的临时副本中 install --frozen-lockfile、peers check、typecheck、85 tests、build、pack 全通过，正常联网安装、未强制 peers。临时副本已删除，回执 artifacts/standalone-result.json。运行代码/依赖与交付版本一致，后续仅文档状态更新。
- CodeGraph：已 sync 并检查 CourseAuthoringService、LearningService publish/submit、EvidenceService.read、registerLearningTools 的 callers/callees；tools → authoring → evidence read / learning commit，未接入 Harness private API。索引不入 Git。
- Harness baseline：upstream `c291e7961a515f6d7af9304e7fd1d257929aef26` / 0.1.5-rc.2；本轮集成 fork `e0f7cd593b269e057416a8b1c568b22174a1b0a2`，随后仅更新发行元数据。packages/apps 相对 upstream **0 changes**，没有更新 upstream。
- Known blockers / unverified：real LLM semantic smoke **NOT RUN: no credentials**。环境变量、两仓 .env、Harness .env 没有模型 key；credentials store 仅 client-connection/browser-session。尚未证明模型自主工具选择、数学提案质量或抗注入行为，P5 final acceptance 前必须跑真实模型问答/引用，最好覆盖 authoring。Node 22 版本矩阵未运行。Agent 生成的答案仍存在 tool-call arguments/session logs，P3 只保证 public projection 隔离，不声称浏览器端完全隐藏。
- Deferred：P4 Quiz/Plan/Progress/Evidence UI；PDF/MinerU/OpenFile（沿用 P2 strict-peer 不兼容结论，本轮未改/未复验）；Docker/P5、外部可选集成。
- Next 3 concrete tasks：① P4 使用公开 dsh.client/slots/tool views 实现课程选择与 MCQ Submit/Feedback，不改 apps/web；② 接 Plan/Progress/Why changed 展示 v1→v2、两条错题证据及 20 min/3 题；③ 凭证可用后执行 golden-path 的真实 LLM grounding/authoring/资料不足/injection semantic acceptance，再做 P5 pinned 交付。
